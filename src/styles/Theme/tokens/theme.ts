import color from 'styles/Theme/tokens/color';
import typography from 'styles/Theme/tokens/typography';
import layout from 'styles/Theme/tokens/layout';
import animation from 'styles/Theme/tokens/animation';
import common from 'styles/Theme/tokens/common';

const theme = {
  color,
  typography,
  layout,
  animation,
  common,
};

export default theme;
